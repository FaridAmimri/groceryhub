export type CategoryType = {
    id: string;
    title: string;
    color: string;
    icon: string;
    slug: string;
}

export type SlideType = {
    id: string;
    name: string;
    image: string;
    type: string;
}